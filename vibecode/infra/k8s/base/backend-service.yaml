apiVersion: v1
kind: Service
metadata:
  name: vibecode-backend
  namespace: vibecode
  labels:
    app.kubernetes.io/name: vibecode-backend
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: vibecode-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vibecode-backend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
